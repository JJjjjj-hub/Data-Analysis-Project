"""
Local-only settings overrides.

This file is intentionally *not* imported by default.
If you want to use it, import it at the bottom of config/settings.py:
    try:
        from .local_settings import *  # noqa
    except ImportError:
        pass
"""

